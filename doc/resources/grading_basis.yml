---
grading_basis:
  attributes:
    required:
      type:
        type: string
        description: "the resource type"
      grading_basis_id:
        type: string
        description: "the resource type"
      description:
        type: string
        description: "the resource type"
