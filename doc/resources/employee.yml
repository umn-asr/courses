---
employee:
  attributes:
    required:
      type:
        type: string
        description: "the resource type"
      name:
        type: string
        description: "the resource type"
      email:
        type: string
        description: "the resource type"
      role:
        type: string
        description: "the resource type"
